hash = {"a": 1, "b": 2, "c": 3}
for k,v in hash {
    print(k, v)
}

hash["f"] = func(x, y) {
    print(x + y)
}
hash["f"](1, 2)

for key in hash.keys() {
    print(key)
}

for val in hash.values() {
    print(val)
}

print(hash.get("x", "x"))

key = "t"
if hash.has(key) {
    printf("key:%s in hash", key)
    print(hash.get(key))
} else {
    printf("key:%s not in hash", key)
}
