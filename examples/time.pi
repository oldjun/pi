
import time

for i = 0; i < 10; i++ {
	time.sleep(1000)
	now = time.time()
	str = time.strftime("%Y-%m-%d %H:%M:%S", now)
	ts = time.strptime(str, "%Y-%m-%d %H:%M:%S")
	print(now, "--", str, "--", ts)
}